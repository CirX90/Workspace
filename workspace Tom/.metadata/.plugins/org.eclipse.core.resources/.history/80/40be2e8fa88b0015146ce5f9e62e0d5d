package aufgabenblatt2;

import java.util.EnumMap;
import java.util.Map;
import java.util.function.BinaryOperator;

public class Rechner {
	
	double zahl1 = 1.0;
	double zahl2 = 2.0;
	
	private enum Operation {ADDIEREN, SUBTRAHIEREN, MULTIPLIZIEREN, DIVIDIEREN}
	
	private VerrechnungZweiDoubles addiere = (zahl1, zahl2) -> {
		return zahl1 + zahl2;
	};
	
	
	static Map<Operation, BinaryOperator> map = new EnumMap<Operation,
			BinaryOperator>(Operation.class);
	
	public double berechne(Operation, double zahl1, double zahl2) {
		
		return 0.0;
		
	}
	
	public static void main (String[] args) {
		
		map.put(Operation.ADDIEREN, "Addition");
		map.put(Operation.SUBTRAHIEREN, "Subtraktion");
		map.put(Operation.MULTIPLIZIEREN, "Multiplikation");
		map.put(Operation.DIVIDIEREN, "Division");
		
//		String value = map.get(Operation.ADDIEREN);
//		System.out.println(value);
	}

}
